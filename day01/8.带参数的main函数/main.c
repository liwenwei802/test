#include <stdio.h>
#include <stdlib.h>

//参数1 表示main函数 传递给参数的总个数
//参数2 是一个数组 数组的每一个元素都是 char*
void main(int argc, char *argv[])
{
	/*for(int i = 0; i < argc;i++)
	{
		printf("%s ", *(argv + i));
	}*/
	system("pause");
}
